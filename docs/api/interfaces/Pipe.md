[Overview](../index.md) / Pipe

# Pipe()\<A, B\>

Interface for a pipable function that allows chaining operations.

## Type Parameters

| Type Parameter | Description |
| ------ | ------ |
| `A` | The input type. |
| `B` | The output type. |

> **Pipe**(`a`): `B`

Interface for a pipable function that allows chaining operations.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `a` | `A` |

## Returns

`B`

## Properties

| Property | Type |
| ------ | ------ |
| <a id="property-pipe"></a> `pipe` | \<`C`\>(`fn2`) => `Pipe`\<`A`, `C`\> |
